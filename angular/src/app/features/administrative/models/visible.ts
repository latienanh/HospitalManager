interface VisibleAdd {
    addStatus: boolean;
    showAddForm: boolean;
  }
interface VisibleImport {
    importStatus: boolean;
    showImportForm: boolean;
}
interface VisibleUpdate {
    updateStatus: boolean;
    showUpdateForm: boolean;
}
export {
    VisibleAdd,
    VisibleImport,
    VisibleUpdate
}